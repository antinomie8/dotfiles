; extends

(table
	(bare_key) @markup.heading)

(table
	(dotted_key
		(bare_key) @markup.heading))

(table_array_element
	(bare_key) @markup.heading)

(table_array_element
	(dotted_key
		(bare_key) @markup.heading))
